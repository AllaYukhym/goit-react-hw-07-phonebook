export const getContacts = state => state.data.contacts;

export const getFilter = state => state.filter.letters;

export const getError = state => state.data.error;

export const getIsLoading = state => state.data.isLoading;
